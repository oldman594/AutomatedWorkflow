{
  "name": "autoflow-desktop",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "check:system": "node scripts/check-system-deps.mjs",
    "predev": "npm run check:system",
    "dev": "node scripts/run-tauri-dev.mjs",
    "prebuild": "npm run check:system",
    "build": "tauri build",
    "check": "cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  }
}
