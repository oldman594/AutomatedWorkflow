# Runner resource

Release builds place the PyInstaller `autoflow-runner` executable in `resources/bin/` before
invoking `tauri build`. Development builds may use an `autoflow-runner` executable from `PATH`.
