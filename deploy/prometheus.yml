global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/autoflow-alerts.yml

scrape_configs:
  - job_name: autoflow
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials_file: /etc/prometheus/metrics-token
    static_configs:
      - targets: ["autoflow:8765"]
